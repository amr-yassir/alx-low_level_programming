#include <stdio.h>

/**
 * main -Entry point
 *
 * Description: print numbers from 0 to 9
 *
 * Return: 0 (success)
*/

int main(void)
{
	for (int i; i < 10; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}
