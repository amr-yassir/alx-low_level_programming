#include "main.h"

/**
 * cap_string - cap all ward
 *
 * @str: input
 *
 * Return: string
*/

char *cap_string(char *str)
{
	int i, j;

	j = 0;
	i = j + 1
	while (str[j] = 32)
	{
		str[i] = str[i] - 32;
		j++;
	}
	return (str);
}
